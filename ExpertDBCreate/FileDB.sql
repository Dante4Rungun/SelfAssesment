CREATE TABLE PSEFiles(
	fileID INTEGER PRIMARY KEY NOT NULL,
	ExpFile varbinary(max)
);
CREATE TABLE SEFiles(
	fileID INTEGER PRIMARY KEY NOT NULL,
	ExpFile varbinary(max) 
);
CREATE TABLE PEFiles(
	fileID INTEGER PRIMARY KEY NOT NULL,
	ExpFile varbinary(max) 
);
CREATE TABLE PHEFiles(
	fileID INTEGER PRIMARY KEY NOT NULL,
	ExpFile varbinary(max) 
);
CREATE TABLE HEFiles(
	fileID INTEGER PRIMARY KEY NOT NULL,
	ExpFile varbinary(max) 
);
CREATE TABLE RPEFiles(
	fileID INTEGER PRIMARY KEY NOT NULL,
	ExpFile varbinary(max) 
);